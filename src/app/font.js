import { Rubik, Open_Sans } from "next/font/google";

export const rubik = Rubik({
	subsets: ["latin"],
	display: "swap",
});
export const opensans = Open_Sans({
	subsets: ["latin"],
	display: "swap",
});
